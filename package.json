{
  "name": "my-nextjs-form-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  },
  "dependencies": {
    "next": "latest", // Or the specific version you want
    "react": "latest", // Or the specific version you want
    "react-dom": "latest" // Or the specific version you want
  },
  "devDependencies": {
    "eslint": "latest", // Optional: for linting
    "eslint-config-next": "latest" // Optional: for linting
  }
}
